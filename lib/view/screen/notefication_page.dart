import 'package:flutter/cupertino.dart';

class NoteficationPage extends StatelessWidget {
  const NoteficationPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("NoteficationPage"),);
  }
}
